{
  "name": "easy-employee-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "@react-leaflet/core": "^1.1.1",
    "react-leaflet": "^3.2.5"
  },
  "devDependencies": {
    "file-loader": "^6.2.0"
  }
}
